<form method="post" action="{{ path('funcionario_delete', {'id': funcionario.id}) }}" onsubmit="return confirm('Você tem certeza que deseja excluir este item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ funcionario.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
