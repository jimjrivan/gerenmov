{% extends 'base.html.twig' %}

{% block title %}GerenMov - Funcionário{% endblock %}

{% block body %}
    <h1>Funcionário</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ funcionario.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ funcionario.nomeStr }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('funcionario_index') }}" class="btn btn-primary">Voltar</a>

    <a href="{{ path('funcionario_edit', {'id': funcionario.id}) }}" class="btn btn-light">Editar</a>

    {{ include('funcionario/_delete_form.html.twig') }}
{% endblock %}
