<form method="post" action="{{ path('movimentos_delete', {'id': movimento.id}) }}" onsubmit="return confirm('Você tem certeza que deseja excluir este item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ movimento.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
