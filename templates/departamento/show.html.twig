{% extends 'base.html.twig' %}

{% block title %}GerenMov - Departamento{% endblock %}

{% block body %}
    <h1>Departamento</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ departamento.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ departamento.nomeStr }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('departamento_index') }}" class="btn btn-primary">Voltar</a>

    <a href="{{ path('departamento_edit', {'id': departamento.id}) }}" class="btn btn-light">Editar</a>

    {{ include('departamento/_delete_form.html.twig') }}
{% endblock %}
