{% extends 'base.html.twig' %}

{% block title %}GerentMov - Departamento{% endblock %}

{% block body %}
    <h1>Editar Departamento</h1>

    {{ include('departamento/_form.html.twig', {'button_label': 'Confirmar'}) }}

    <a href="{{ path('departamento_index') }}" class="btn btn-primary">Voltar</a>

    {{ include('departamento/_delete_form.html.twig') }}
{% endblock %}
